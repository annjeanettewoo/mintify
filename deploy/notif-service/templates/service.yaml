apiVersion: v1
kind: Service
metadata:
  name: {{ include "notif-service.fullname" . }}
  labels:
    {{- include "notif-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "notif-service.selectorLabels" . | nindent 4 }}
